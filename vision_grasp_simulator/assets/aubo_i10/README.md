# AUBO i10 模型目录

请将 `aubo i10 meshes` 中的 `aubo_i10.xacro`、`aubo_i10_gazebo_roscontrol.urdf` 和 `meshes/` 复制到本目录。程序会解析 URDF 的 link、joint、origin、axis、limit 与 visual mesh 信息，并将 `package://aubo_description/meshes/aubo_i10/` 映射到本目录下的 `meshes/`。
