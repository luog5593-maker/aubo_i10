# sample_data

本目录不提交 PNG/JPG/BMP 等二进制示例图片，避免创建拉取请求时出现“不支持二进制文件”的问题。

软件运行后，点击界面左侧“生成示例图像”按钮，程序会通过 `utils/image_utils.py` 中的 `generate_demo_image()` 函数自动生成包含“膜库组件”和“夹具”的仿真示例图，并在本地运行目录生成临时图片文件。该运行时生成的图片会被 `.gitignore` 忽略，不纳入版本控制。
